/*
 * DO NOT EDIT THIS FILE. Generated by checkmk.
 * Edit the original source file "test.check" instead.
 */

#include <check.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "../headers/my_string.h"

START_TEST(memcmp1) {
  // Equal strings
  char *str1 = "Hello";
  char *str2 = "Hello";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_int_eq(memcmp(str1, str2, n), my_memcmp(str1, str2, m));
}
END_TEST

START_TEST(memcmp2) {
  // Different strings1
  char *str1 = "Hello22343746387r56873458937";
  char *str2 = "aello3";
  size_t n = 7;
  my_size_t m = 7;
  ck_assert_int_eq(memcmp(str1, str2, n) < 0, my_memcmp(str1, str2, m) < 0);
}
END_TEST

START_TEST(memcmp3) {
  // Different strings2
  char *str1 = "Hello1234";
  char *str2 = "Hello";
  size_t n = 6;
  my_size_t m = 6;
  ck_assert_int_eq(memcmp(str1, str2, n) > 0, my_memcmp(str1, str2, m) > 0);
}
END_TEST

START_TEST(memcmp4) {
  // Null string1
  char *str1 = "";
  char *str2 = "Hello1234";
  size_t n = 6;
  my_size_t m = 6;
  ck_assert_int_eq(memcmp(str1, str2, n) < 0, my_memcmp(str1, str2, m) < 0);
}
END_TEST

START_TEST(memcmp5) {
  // Null string2
  char *str1 = "Hello";
  char *str2 = "";
  size_t n = 6;
  my_size_t m = 6;
  ck_assert_int_eq(memcmp(str1, str2, n) > 0, my_memcmp(str1, str2, m) > 0);
}
END_TEST

START_TEST(memcmp6) {
  // Null strings
  char *str1 = "";
  char *str2 = "";
  size_t n = 6;
  my_size_t m = 6;
  ck_assert_int_eq(memcmp(str1, str2, n), my_memcmp(str1, str2, m));
}
END_TEST

START_TEST(memcmp7) {
  // Null size
  char *str1 = "Hello";
  char *str2 = "Hello1234";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_int_eq(memcmp(str1, str2, n), my_memcmp(str1, str2, m));
}
END_TEST

START_TEST(strcmp1) {
  // Equal strings
  const char *str1 = "Hello";
  const char *str2 = "Hello";
  ck_assert_int_eq(strcmp(str1, str2), my_strcmp(str1, str2));
}
END_TEST

START_TEST(strcmp2) {
  // Different strings1
  const char *str1 = "Hello22343746387r56873458937";
  const char *str2 = "aello3";
  ck_assert_int_eq(strcmp(str1, str2) < 0, my_strcmp(str1, str2) < 0);
}
END_TEST

START_TEST(strcmp3) {
  // Different strings2
  const char *str1 = "Hello1234";
  const char *str2 = "Hello";
  ck_assert_int_eq(strcmp(str1, str2) > 0, my_strcmp(str1, str2) > 0);
}
END_TEST

START_TEST(strcmp4) {
  // Null string1
  const char *str1 = "";
  const char *str2 = "Hello1234";
  ck_assert_int_eq(strcmp(str1, str2) < 0, my_strcmp(str1, str2) < 0);
}
END_TEST

START_TEST(strcmp5) {
  // Null string2
  const char *str1 = "Hello";
  const char *str2 = "";
  ck_assert_int_eq(strcmp(str1, str2) > 0, my_strcmp(str1, str2) > 0);
}
END_TEST

START_TEST(strcmp6) {
  // Null strings
  const char *str1 = "\0";
  const char *str2 = "\0";
  ck_assert_int_eq(strcmp(str1, str2), my_strcmp(str1, str2));
}
END_TEST

START_TEST(strncmp1) {
  // Equal strings
  const char *str1 = "Hello";
  const char *str2 = "Hello";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_int_eq(strncmp(str1, str2, n), my_strncmp(str1, str2, m));
}
END_TEST

START_TEST(strncmp2) {
  // Different strings1
  const char *str1 = "Hello22343746387r56873458937";
  const char *str2 = "aello3";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_int_eq(strncmp(str1, str2, n) < 0, my_strncmp(str1, str2, m) < 0);
}
END_TEST

START_TEST(strncmp3) {
  // Different strings2
  const char *str1 = "Hello1234";
  const char *str2 = "Hello";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_int_eq(strncmp(str1, str2, n), my_strncmp(str1, str2, m));
}
END_TEST

START_TEST(strncmp4) {
  // Null string1
  const char *str1 = "";
  const char *str2 = "Hello1234";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_int_eq(strncmp(str1, str2, n) < 0, my_strncmp(str1, str2, m) < 0);
}
END_TEST

START_TEST(strncmp5) {
  // Null string2
  const char *str1 = "Hello";
  const char *str2 = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_int_eq(strncmp(str1, str2, n) > 0, my_strncmp(str1, str2, m) > 0);
}
END_TEST

START_TEST(strncmp6) {
  // Null strings
  const char *str1 = "\0";
  const char *str2 = "\0";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_int_eq(strncmp(str1, str2, n), my_strncmp(str1, str2, m));
}
END_TEST

START_TEST(strncmp7) {
  // Null size
  const char *str1 = "Hello";
  const char *str2 = "Hel";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_int_eq(strncmp(str1, str2, n), my_strncmp(str1, str2, m));
}
END_TEST

START_TEST(memcpy1) {
  // Full string, length > n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "world";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy2) {
  // Full string, length < n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "wo";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy3) {
  // Full string, length = 0
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy4) {
  // Null string1, length > n
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "world";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy5) {
  // Null string1, length < n
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "wo";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy6) {
  // Null string1, length = 0
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy7) {
  // Null string2, length > n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy8) {
  // Null string2, length < n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy9) {
  // Null string2, length = 0
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy10) {
  // Null strings, n > 0
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy11) {
  // Null strings, n = 0
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy12) {
  // Full string, size1 < size2, length = 0
  char str1[4] = "Hel";
  char str3[4] = "Hel";
  char str2[20] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memcpy13) {
  // Null string1, size1 < size2, length = 0
  char str1[3] = "";
  char str3[3] = "";
  char str2[20] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memcpy(str1, str2, n), my_memcpy(str3, str2, m));
}
END_TEST

START_TEST(memmove1) {
  // Full string, length > n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "world";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove2) {
  // Full string, length < n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "wo";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove3) {
  // Full string, length = 0
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove4) {
  // Null string1, length > n
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "world";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove5) {
  // Null string1, length < n
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "wo";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove6) {
  // Null string1, length = 0
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove7) {
  // Null string2, length > n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove8) {
  // Null string2, length < n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove9) {
  // Null string2, length = 0
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[20] = "";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove10) {
  // Null strings, n > 0
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove11) {
  // Null strings, n = 0
  char str1[20] = "";
  char str3[20] = "";
  char str2[20] = "";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove12) {
  // Full string, size1 < size2, length = 0
  char str1[4] = "Hel";
  char str3[4] = "Hel";
  char str2[20] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memmove13) {
  // Null string1, size1 < size2, length = 0
  char str1[3] = "";
  char str3[3] = "";
  char str2[20] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memmove(str1, str2, n), my_memmove(str3, str2, m));
}
END_TEST

START_TEST(memset1) {
  // Full string, size > n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char c = 'g';
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memset(str1, c, n), my_memset(str3, c, m));
}
END_TEST

START_TEST(memset2) {
  // Full string, size = 0
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char c = 'g';
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memset(str1, c, n), my_memset(str3, c, m));
}
END_TEST

START_TEST(memset3) {
  // Null string, size > n
  char str1[20] = "";
  char str3[20] = "";
  char c = 'g';
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(memset(str1, c, n), my_memset(str3, c, m));
}
END_TEST

START_TEST(memset4) {
  // Null string, size = 0
  char str1[20] = "";
  char str3[20] = "";
  char c = 'g';
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(memset(str1, c, n), my_memset(str3, c, m));
}
END_TEST

START_TEST(strcpy1) {
  // Full string
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[10] = "world";
  ck_assert_str_eq(strcpy(str1, str2), my_strcpy(str3, str2));
}
END_TEST

START_TEST(strcpy2) {
  // Empty string1
  char str1[20] = "";
  char str3[20] = "";
  char str2[10] = "world";
  ck_assert_str_eq(strcpy(str1, str2), my_strcpy(str3, str2));
}
END_TEST

START_TEST(strcpy3) {
  // Empty string2
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[10] = "";
  ck_assert_str_eq(strcpy(str1, str2), my_strcpy(str3, str2));
}
END_TEST

START_TEST(strcpy4) {
  // Null strings
  char str1[20] = "";
  char str3[20] = "";
  char str2[10] = "";
  ck_assert_str_eq(strcpy(str1, str2), my_strcpy(str3, str2));
}
END_TEST

START_TEST(strncpy3) {
  // Null string1, size > n
  char str1[20] = "";
  char str3[20] = "";
  char str2[10] = "world";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(strncpy(str1, str2, n), my_strncpy(str3, str2, m));
}
END_TEST

START_TEST(strncpy4) {
  // Null string1, size = 0
  char str1[20] = "";
  char str3[20] = "";
  char str2[10] = "world";
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_str_eq(strncpy(str1, str2, n), my_strncpy(str3, str2, m));
}
END_TEST

START_TEST(strncpy5) {
  // Null string2, size > n
  char str1[20] = "Hello";
  char str3[20] = "Hello";
  char str2[10] = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(strncpy(str1, str2, n), my_strncpy(str3, str2, m));
}
END_TEST

START_TEST(strncpy7) {
  // Null strings, size > n
  char str1[20] = "";
  char str3[20] = "";
  char str2[10] = "";
  size_t n = 4;
  my_size_t m = 4;
  ck_assert_str_eq(strncpy(str1, str2, n), my_strncpy(str3, str2, m));
}
END_TEST

START_TEST(strlen1) {
  // Full string
  char str1[20] = "Hello";
  ck_assert_int_eq(strlen(str1), my_strlen(str1));
}
END_TEST

START_TEST(strlen2) {
  // String1 > string2
  char str1[20] = "Hello325245245";
  ck_assert_int_eq(strlen(str1), my_strlen(str1));
}
END_TEST

START_TEST(strlen3) {
  // Empty string
  char str1[20] = "";
  ck_assert_int_eq(strlen(str1), my_strlen(str1));
}
END_TEST

START_TEST(strspn1) {
  // Same strings
  char str1[20] = "Hello";
  char str2[20] = "Hello";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn2) {
  // String1 have some litteral from string2 (start position 1)
  char str1[20] = "Hello325245245";
  char str2[20] = "Hel";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn3) {
  // String1 have some litteral from string2 (start position 2)
  char str1[20] = "Hello325245245";
  char str2[20] = "elH";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn4) {
  // String1 have some litteral from string2 (middle position 1)
  char str1[20] = "Hello325245245";
  char str2[20] = "325";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn5) {
  // String1 have some litteral from string2 (middle position 2)
  char str1[20] = "Hello325245245";
  char str2[20] = "42o";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn6) {
  // String1 have some litteral from string2 (end position 1)
  char str1[20] = "Hello3252452kgf";
  char str2[20] = "kgf";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn7) {
  // String1 have some litteral from string2 (end position 2)
  char str1[20] = "Hello325245245kgf";
  char str2[20] = "fgk";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn8) {
  // String1 don't have string2
  char str1[20] = "Hello";
  char str2[20] = "235676";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn9) {
  // Empty string2
  char str1[20] = "Hello";
  char str2[20] = "";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn10) {
  // Empty string1
  char str1[20] = "";
  char str2[20] = "Hello";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strspn11) {
  // Empty strings
  char str1[20] = "";
  char str2[20] = "";
  ck_assert_int_eq(strspn(str1, str2), my_strspn(str1, str2));
}
END_TEST

START_TEST(strcspn1) {
  // Same strings
  char str1[20] = "Hello";
  char str2[20] = "Hello";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn2) {
  // String1 have some litteral from string2 (start position 1)
  char str1[20] = "Hello325245245";
  char str2[20] = "Hel";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn3) {
  // String1 have some litteral from string2 (start position 2)
  char str1[20] = "Hello325245245";
  char str2[20] = "elH";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn4) {
  // String1 have some litteral from string2 (middle position 1)
  char str1[20] = "Hello325245245";
  char str2[20] = "325";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn5) {
  // String1 have some litteral from string2 (middle position 2)
  char str1[20] = "Hello325245245";
  char str2[20] = "42o";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn6) {
  // String1 have some litteral from string2 (end position 1)
  char str1[20] = "Hello3252452kgf";
  char str2[20] = "kgf";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn7) {
  // String1 have some litteral from string2 (end position 2)
  char str1[20] = "Hello325245245kgf";
  char str2[20] = "fgk";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn8) {
  // String1 don't have string2
  char str1[20] = "Hello";
  char str2[20] = "235676";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn9) {
  // Empty string2
  char str1[20] = "Hello";
  char str2[20] = "";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn10) {
  // Empty string1
  char str1[20] = "";
  char str2[20] = "Hello";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(strcspn11) {
  // Empty strings
  char str1[20] = "";
  char str2[20] = "";
  ck_assert_int_eq(strcspn(str1, str2), my_strcspn(str1, str2));
}
END_TEST

START_TEST(memchr1) {
  // String have litteral (start position)
  char str1[20] = "Hello";
  int c = 'H';
  size_t n = 5;
  my_size_t m = 5;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr2) {
  // String have litteral (middle position)
  char str1[20] = "Hello";
  int c = 'l';
  size_t n = 5;
  my_size_t m = 5;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr3) {
  // String have litteral (end position)
  char str1[20] = "Hello";
  int c = 'o';
  size_t n = 5;
  my_size_t m = 5;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr4) {
  // String have litteral (middle position 2)
  char str1[20] = "Hello";
  int c = 'l';
  size_t n = 1;
  my_size_t m = 1;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr5) {
  // String have litteral (end position 2)
  char str1[20] = "Hello";
  int c = 'o';
  size_t n = 1;
  my_size_t m = 1;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr6) {
  // String don't have litteral 1
  char str1[20] = "Hello";
  int c = 'm';
  size_t n = 5;
  my_size_t m = 5;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr7) {
  // String don't have litteral 2
  char str1[20] = "Hello";
  int c = 'm';
  size_t n = 1;
  my_size_t m = 1;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr8) {
  // String don't have litteral 3
  char str1[20] = "Hello";
  int c = 'r';
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr9) {
  // String have many litterals
  char str1[20] = "Helolalalflglldk";
  int c = 'l';
  size_t n = 35;
  my_size_t m = 35;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr10) {
  // Empty string
  char str1[20] = "\0";
  int c = 'l';
  size_t n = 0;
  my_size_t m = 0;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(memchr11) {
  // Empty string and litteral
  char str1[20] = "";
  int c = '\0';
  size_t n = 35;
  my_size_t m = 35;
  ck_assert_ptr_eq(memchr(str1, c, n), my_memchr(str1, c, m));
}
END_TEST

START_TEST(strchr1) {
  // String have litteral (start position)
  char str1[20] = "Hello";
  int c = 'H';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strchr2) {
  // String don't have litteral (start position)
  char str1[20] = "Hello";
  int c = 'm';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strchr3) {
  // String have many litterals (start position)
  char str1[20] = "HeHllHo";
  int c = 'H';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strchr4) {
  // String have litteral (middle position)
  char str1[20] = "Helo";
  int c = 'l';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strchr5) {
  // String have many litterals (start position)
  char str1[20] = "HelkklmklHo";
  int c = 'l';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strchr6) {
  // String have litteral (end position)
  char str1[20] = "Hello";
  int c = 'o';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strchr7) {
  // String have many litterals (start position)
  char str1[20] = "HelkklmklHoooooo";
  int c = 'o';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strchr8) {
  // Empty string
  char str1[20] = "";
  int c = 'o';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strchr9) {
  // Empty string and litteral
  char str1[20] = "";
  int c = '\0';
  ck_assert_ptr_eq(strchr(str1, c), my_strchr(str1, c));
}
END_TEST

START_TEST(strstr1) {
  // String1 have string2 (start position)
  char str1[50] = "Hello world and bitches";
  char str2[25] = "Hell";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strstr2) {
  // String1 have string2 (middle position)
  char str1[50] = "Hello world and bitches";
  char str2[25] = " and ";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strstr3) {
  // String1 have string2 (end position)
  char str1[50] = "Hello world and bitches";
  char str2[25] = "tch";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strstr4) {
  // String1 have string2 twice
  char str1[50] = "Hello world and bitches and Aboba";
  char str2[25] = " and ";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strstr5) {
  // String1 don't have string2
  char str1[50] = "Hello world and bitches and Aboba";
  char str2[25] = " aNd ";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strstr6) {
  // Empty string2
  char str1[50] = "Hello world and bitches and Aboba";
  char str2[25] = "";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strstr7) {
  // Empty string1
  char str1[50] = "";
  char str2[25] = "Hello Aboba ";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strstr8) {
  // Empty strings
  char str1[50] = "";
  char str2[25] = "";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strstr9) {
  // str2 > str1
  char str1[50] = "Hello";
  char str2[25] = "Hello1";
  ck_assert_ptr_eq(strstr(str1, str2), my_strstr(str1, str2));
}
END_TEST

START_TEST(strrchr1) {
  // String1 have litteral
  char str1[50] = "Hello world";
  char c = 'l';
  ck_assert_ptr_eq(strrchr(str1, c), my_strrchr(str1, c));
}
END_TEST

START_TEST(strrchr2) {
  // String1 don't have litteral
  char str1[50] = "Hello world";
  char c = 'm';
  ck_assert_ptr_eq(strrchr(str1, c), my_strrchr(str1, c));
}
END_TEST

START_TEST(strrchr3) {
  // String1 don't have space
  char str1[50] = "Hello world";
  char c = ' ';
  ck_assert_ptr_eq(strrchr(str1, c), my_strrchr(str1, c));
}
END_TEST

START_TEST(strrchr4) {
  // Null litteral
  char str1[50] = "Hello world";
  char c = '\0';
  ck_assert_ptr_eq(strrchr(str1, c), my_strrchr(str1, c));
}
END_TEST

START_TEST(strrchr5) {
  // Null string
  char str1[50] = "";
  char c = 'G';
  ck_assert_ptr_eq(strrchr(str1, c), my_strrchr(str1, c));
}
END_TEST

START_TEST(strrchr6) {
  // Null string and null litteral
  char str1[50] = "";
  char c = '\0';
  ck_assert_ptr_eq(strrchr(str1, c), my_strrchr(str1, c));
}
END_TEST

START_TEST(strpbrk1) {
  // String1 have litterals from string2
  char str1[50] = "Hello world and bitches";
  char str2[50] = "ablu";
  ck_assert_ptr_eq(strpbrk(str1, str2), my_strpbrk(str1, str2));
}
END_TEST

START_TEST(strpbrk2) {
  // String1 have litterals from string2 with space
  char str1[50] = "Hello world and bitches";
  char str2[50] = "ab lu";
  ck_assert_ptr_eq(strpbrk(str1, str2), my_strpbrk(str1, str2));
}
END_TEST

START_TEST(strpbrk3) {
  // String1 don't have litterals from string2
  char str1[50] = "Hello world and bitches";
  char str2[50] = "1234";
  ck_assert_ptr_eq(strpbrk(str1, str2), my_strpbrk(str1, str2));
}
END_TEST

START_TEST(strpbrk4) {
  // Empty string2
  char str1[50] = "Hello world and bitches";
  char str2[50] = "";
  ck_assert_ptr_eq(strpbrk(str1, str2), my_strpbrk(str1, str2));
}
END_TEST

START_TEST(strpbrk5) {
  // Empty string1
  char str1[50] = "";
  char str2[50] = "1234 567";
  ck_assert_ptr_eq(strpbrk(str1, str2), my_strpbrk(str1, str2));
}
END_TEST

START_TEST(strpbrk6) {
  // Empty strings
  char str1[50] = "";
  char str2[50] = "";
  ck_assert_ptr_eq(strpbrk(str1, str2), my_strpbrk(str1, str2));
}
END_TEST

START_TEST(sprintf_procent1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %%";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_procent2) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %- +#0%";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_procent3) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %- +#0%%%";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_procent4) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %";
  ck_assert_int_eq(sprintf(str, string, 123), my_sprintf(str1, string, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_d1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %d";
  ck_assert_int_eq(sprintf(str, string, 123), my_sprintf(str1, string, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_d2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec d %";
    strcat(string, flag[i]);
    strcat(string, "d");
    ck_assert_int_eq(sprintf(str, string, 123), my_sprintf(str1, string, 123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_d3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec d %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "d");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_d4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec d %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "d");
        ck_assert_int_eq(sprintf(str, string, 123),
                         my_sprintf(str1, string, 123));
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec d %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "d");
          ck_assert_int_eq(sprintf(str, string, 123),
                           my_sprintf(str1, string, 123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %-+ 0#d";
  ck_assert_int_eq(sprintf(str, string, 123), my_sprintf(str1, string, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_d7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec d %";
    strcat(string, num[i]);
    strcat(string, "d");
    ck_assert_int_eq(sprintf(str, string, 123), my_sprintf(str1, string, 123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_d8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec d %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "d");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_d9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec d %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "d");
        ck_assert_int_eq(sprintf(str, string, 123),
                         my_sprintf(str1, string, 123));
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec d %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "d");
          ck_assert_int_eq(sprintf(str, string, 123),
                           my_sprintf(str1, string, 123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec d %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "d");
            ck_assert_int_eq(sprintf(str, string, 123),
                             my_sprintf(str1, string, 123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec d %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "d");
    ck_assert_int_eq(sprintf(str, string, 123), my_sprintf(str1, string, 123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_d13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec d %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "d");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_d14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec d %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "d");
        ck_assert_int_eq(sprintf(str, string, 123),
                         my_sprintf(str1, string, 123));
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec d %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "d");
          ck_assert_int_eq(sprintf(str, string, 123),
                           my_sprintf(str1, string, 123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec d %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "d");
            ck_assert_int_eq(sprintf(str, string, 123),
                             my_sprintf(str1, string, 123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec d %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "d");
              ck_assert_int_eq(sprintf(str, string, 123),
                               my_sprintf(str1, string, 123));
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_d18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][3] = {"10", "3", "8"};
  char precision[4][3] = {".0", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec d %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "d");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_d19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %*.*d";
  ck_assert_int_eq(sprintf(str, string, 10, 10, 123),
                   my_sprintf(str1, string, 10, 10, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_d20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %-+ 0#*.*d";
  ck_assert_int_eq(sprintf(str, string, 10, 10, 123),
                   my_sprintf(str1, string, 10, 10, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_d21) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char flag[2][2] = {"h", "l"};
  for (int i = 0; i < 2; i++) {
    char string[100] = "Check spec d %";
    strcat(string, flag[i]);
    strcat(string, "d");
    sprintf(str, string, 123456);
    my_sprintf(str1, string, 123456);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_d22) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %10.d";
  sprintf(str, string, 0);
  my_sprintf(str1, string, 0);
  ck_assert_str_eq(str, str1);
  memset(str, '\0', 100);
  memset(str1, '\0', 100);
}

END_TEST
START_TEST(sprintf_d23) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec d %10.d %d";
  sprintf(str, string, 0, INT32_MAX);
  my_sprintf(str1, string, 0, INT32_MAX);
  ck_assert_int_eq(sprintf(str, string, 0, INT32_MAX),
                   my_sprintf(str1, string, 0, INT32_MAX));
  ck_assert_str_eq(str, str1);
  memset(str, '\0', 100);
  memset(str1, '\0', 100);
}

END_TEST

START_TEST(sprintf_i1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec i %i";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_i2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec i %";
    strcat(string, flag[i]);
    strcat(string, "i");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_i3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec i %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "i");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_i4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec i %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "i");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec i %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "i");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec i %-+ 0#d";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_i7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec i %";
    strcat(string, num[i]);
    strcat(string, "i");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_i8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec i %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "i");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_i9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec i %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "i");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec i %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "i");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec i %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "i");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec i %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "i");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_i13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec i %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "i");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_i14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec i %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "i");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec i %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "i");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec i %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "i");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec i %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "i");
              sprintf(str, string, 123);
              my_sprintf(str1, string, 123);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_i18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec i %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "i");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_i19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec i %*.*i";
  ck_assert_int_eq(sprintf(str, string, 10, 10, 123),
                   my_sprintf(str1, string, 10, 10, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_i20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec i %-+ 0#*.*i";
  ck_assert_int_eq(sprintf(str, string, 10, 10, 123),
                   my_sprintf(str1, string, 10, 10, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_i21) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char flag[2][2] = {"h", "l"};
  for (int i = 0; i < 2; i++) {
    char string[100] = "Check spec i %";
    strcat(string, flag[i]);
    strcat(string, "i");
    sprintf(str, string, 123456);
    my_sprintf(str1, string, 123456);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST
START_TEST(sprintf_i22) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][3] = {"10", "20", "8"};
  char precision[4][4] = {".0", ".8", ".12", ".15"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec i %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "i");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_x1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec x %x";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_x2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec x %";
    strcat(string, flag[i]);
    strcat(string, "x");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_x3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec x %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "x");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_x4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec x %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "x");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec x %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "x");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec x %-+ 0#d";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_x7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec x %";
    strcat(string, num[i]);
    strcat(string, "x");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_x8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec x %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "x");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_x9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec x %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "x");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec x %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "x");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec x %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "x");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec x %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "x");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_x13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec x %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "x");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_x14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec x %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "x");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec x %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "x");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec x %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "x");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec x %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "x");
              sprintf(str, string, 123);
              my_sprintf(str1, string, 123);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_x18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec x %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "x");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_x19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec x %*.*x";
  ck_assert_int_eq(sprintf(str, string, 10, 10, 123),
                   my_sprintf(str1, string, 10, 10, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_x20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec x %-+ 0#*.*x";
  ck_assert_int_eq(sprintf(str, string, 10, 10, 123),
                   my_sprintf(str1, string, 10, 10, 123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_x21) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char flag[2][2] = {"h", "l"};
  for (int i = 0; i < 2; i++) {
    char string[100] = "Check spec x %";
    strcat(string, flag[i]);
    strcat(string, "x");
    sprintf(str, string, 123456);
    my_sprintf(str1, string, 123456);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_X1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec X %X";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_X2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec X %";
    strcat(string, flag[i]);
    strcat(string, "X");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_X3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec X %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "X");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_X4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec X %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "X");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec X %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "X");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec X %-+ 0#d";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_X7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec X %";
    strcat(string, num[i]);
    strcat(string, "X");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_X8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec X %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "X");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_X9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec X %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "X");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec X %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "X");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec X %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "X");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec X %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "X");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_X13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec X %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "X");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_X14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec X %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "X");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec X %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "X");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec X %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "X");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec X %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "X");
              sprintf(str, string, 123);
              my_sprintf(str1, string, 123);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_X18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec X %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "X");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_X19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec X %*.*X";
  sprintf(str, string, 10, 10, 123);
  my_sprintf(str1, string, 10, 10, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_X20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec X %-+ 0#*.*X";
  sprintf(str, string, 10, 10, 123);
  my_sprintf(str1, string, 10, 10, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_X21) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char flag[2][2] = {"h", "l"};
  for (int i = 0; i < 2; i++) {
    char string[100] = "Check spec X %";
    strcat(string, flag[i]);
    strcat(string, "X");
    sprintf(str, string, 123456);
    my_sprintf(str1, string, 123456);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_u1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec u %u";
  ck_assert_int_eq(sprintf(str, string, -123), my_sprintf(str1, string, -123));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_u2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec u %";
    strcat(string, flag[i]);
    strcat(string, "u");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_u3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec u %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "u");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_u4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec u %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "u");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec u %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "u");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec u %-+ 0#d";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_u7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec u %";
    strcat(string, num[i]);
    strcat(string, "u");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_u8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec u %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "u");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_u9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec u %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "u");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec u %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "u");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec u %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "u");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec u %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "u");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_u13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec u %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "u");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_u14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec u %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "u");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec u %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "u");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec u %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "u");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec u %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "u");
              sprintf(str, string, 123);
              my_sprintf(str1, string, 123);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_u18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec u %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "u");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_u19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec u %*.*u";
  sprintf(str, string, 10, 10, 123);
  my_sprintf(str1, string, 10, 10, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_u20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec u %-+ 0#*.*u";
  sprintf(str, string, 10, 10, 123);
  my_sprintf(str1, string, 10, 10, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_u21) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char flag[2][2] = {"h", "l"};
  for (int i = 0; i < 2; i++) {
    char string[100] = "Check spec u %";
    strcat(string, flag[i]);
    strcat(string, "u");
    sprintf(str, string, 123456);
    my_sprintf(str1, string, 123456);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_u22) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec u %10.u";
  sprintf(str, string, 0);
  my_sprintf(str1, string, 0);
  ck_assert_str_eq(str, str1);
  memset(str, '\0', 100);
  memset(str1, '\0', 100);
}
END_TEST

START_TEST(sprintf_o1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec o %o";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_o2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec o %";
    strcat(string, flag[i]);
    strcat(string, "o");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_o3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec o %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "o");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_o4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec o %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "o");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec o %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "o");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec o %-+ 0#d";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_o7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec o %";
    strcat(string, num[i]);
    strcat(string, "o");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_o8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec o %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "o");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_o9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec o %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "o");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec o %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "o");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec o %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "o");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec o %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "o");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_o13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec o %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "o");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_o14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec o %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "o");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec o %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "o");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec o %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "o");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec o %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "o");
              sprintf(str, string, 123);
              my_sprintf(str1, string, 123);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_o18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec o %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "o");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_o19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec o %*.*o";
  sprintf(str, string, 10, 10, 123);
  my_sprintf(str1, string, 10, 10, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_o20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec o %-+ 0#*.*o";
  sprintf(str, string, 10, 10, 123);
  my_sprintf(str1, string, 10, 10, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_o21) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char flag[2][2] = {"h", "l"};
  for (int i = 0; i < 2; i++) {
    char string[100] = "Check spec o %";
    strcat(string, flag[i]);
    strcat(string, "o");
    sprintf(str, string, 123456);
    my_sprintf(str1, string, 123456);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_p1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec p %p";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_p2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec p %";
    strcat(string, flag[i]);
    strcat(string, "p");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_p3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec p %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "p");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_p4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec p %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "p");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec p %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "p");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec p %-+ 0#d";
  sprintf(str, string, 123);
  my_sprintf(str1, string, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_p7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec p %";
    strcat(string, num[i]);
    strcat(string, "p");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_p8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec p %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "p");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_p9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec p %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "p");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec p %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "p");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec p %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "p");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec p %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "p");
    ck_assert_int_eq(sprintf(str, string, -123),
                     my_sprintf(str1, string, -123));
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_p13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec p %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "p");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_p14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec p %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "p");
        sprintf(str, string, 123);
        my_sprintf(str1, string, 123);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec p %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "p");
          ck_assert_int_eq(sprintf(str, string, -123),
                           my_sprintf(str1, string, -123));
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec p %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "p");
            ck_assert_int_eq(sprintf(str, string, -123),
                             my_sprintf(str1, string, -123));
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec p %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "p");
              sprintf(str, string, 123);
              my_sprintf(str1, string, 123);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_p18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][3] = {"1", "3", "11"};
  char precision[4][4] = {".", ".1", ".3", ".11"};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec p %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "p");
      ck_assert_int_eq(sprintf(str, string, 123),
                       my_sprintf(str1, string, 123));
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_p19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec p %*.*p";
  sprintf(str, string, 10, 10, 123);
  my_sprintf(str1, string, 10, 10, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_p20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec p %-+ 0#*.*p";
  sprintf(str, string, 10, 10, 123);
  my_sprintf(str1, string, 10, 10, 123);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_p21) {
  // Flags h and l
  char str[100] = {0};
  char str1[100] = {0};
  char flag[2][2] = {"h", "l"};
  for (int i = 0; i < 2; i++) {
    char string[100] = "Check spec p %";
    strcat(string, flag[i]);
    strcat(string, "p");
    int p = 123456;
    sprintf(str, string, p);
    my_sprintf(str1, string, p);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_p22) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  float *p = my_NULL;
  char string[100] = "Check spec p %-+ 0#*.*p";
  sprintf(str, string, 10, 10, p);
  my_sprintf(str1, string, 10, 10, p);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_procent_1) {
  // %
  char str[100] = {0};
  char str1[100] = {0};
  char sym = 'h';
  char string[100] = "Check spec p %% and %c ok";
  sprintf(str, string, sym);
  my_sprintf(str1, string, sym);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_n) {
  // %
  char str[100] = {0};
  char str1[100] = {0};
  char sym = 'h';
  int res = 0;
  int res1 = 0;
  char string[100] = "Check spec p %% %n and %c ok";
  sprintf(str, "Check spec p %% %n and %c ok", &res, sym);
  my_sprintf(str1, "Check spec p %% %n and %c ok", &res1, sym);
  ck_assert_int_eq(res, res1);
}
END_TEST

START_TEST(sprintf_f1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec f %f";
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, num[i]);
    my_sprintf(str1, string, num[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_f2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec f %";
    strcat(string, flag[i]);
    strcat(string, "f");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, num[j]);
      my_sprintf(str1, string, num[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_f3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec f %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "f");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, num[k]);
        my_sprintf(str1, string, num[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_f4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec f %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "f");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, num[l]);
          my_sprintf(str1, string, num[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec f %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "f");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, num[m]);
            my_sprintf(str1, string, num[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec f %-+ 0#f";
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, num[i]);
    my_sprintf(str1, string, num[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_f7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.623, 123456789.623456};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec f %";
    strcat(string, num[i]);
    strcat(string, "f");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, arg[j]);
      my_sprintf(str1, string, arg[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_f8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, -123456789.123456};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec f %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "f");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_f9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec f %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "f");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, arg[l]);
          my_sprintf(str1, string, arg[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, -0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec f %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "f");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, arg[m]);
            my_sprintf(str1, string, arg[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec f %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "f");
            for (int n = 0; n < 5; n++) {
              sprintf(str, string, arg[n]);
              my_sprintf(str1, string, arg[n]);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
            }
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec f %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "f");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, arg[j]);
      my_sprintf(str1, string, arg[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_f13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec f %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "f");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_f14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec f %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "f");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, arg[l]);
          my_sprintf(str1, string, arg[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec f %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "f");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, arg[m]);
            my_sprintf(str1, string, arg[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, -1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec f %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "f");
            for (int n = 0; n < 5; n++) {
              sprintf(str, string, arg[n]);
              my_sprintf(str1, string, arg[n]);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
            }
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec f %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "f");
              for (int p = 0; p < 5; p++) {
                sprintf(str, string, arg[p]);
                my_sprintf(str1, string, arg[p]);
                ck_assert_str_eq(str, str1);
                memset(str, '\0', 100);
                memset(str1, '\0', 100);
              }
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_f18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec f %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "f");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_f19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec f %*.*f";
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, 10, 10, arg[i]);
    my_sprintf(str1, string, 10, 10, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_f20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec f %-+ 0#*.*f";
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, 10, 10, arg[i]);
    my_sprintf(str1, string, 10, 10, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_f21) {
  // Flag L
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec f %Lf";
  long double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, arg[i]);
    my_sprintf(str1, string, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_e1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec e %e";
  double num[5] = {0.0, 0.00164, -0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, num[i]);
    my_sprintf(str1, string, num[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_e2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec e %";
    strcat(string, flag[i]);
    strcat(string, "e");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, num[j]);
      my_sprintf(str1, string, num[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_e3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec e %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "e");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, num[k]);
        my_sprintf(str1, string, num[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_e4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec e %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "e");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, num[l]);
          my_sprintf(str1, string, num[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, -1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec e %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "e");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, num[m]);
            my_sprintf(str1, string, num[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec e %-+ 0#e";
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, num[i]);
    my_sprintf(str1, string, num[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_e7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec e %";
    strcat(string, num[i]);
    strcat(string, "e");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, arg[j]);
      my_sprintf(str1, string, arg[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_e8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec e %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "e");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_e9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, -0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec e %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "e");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, arg[l]);
          my_sprintf(str1, string, arg[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec e %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "e");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, arg[m]);
            my_sprintf(str1, string, arg[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec e %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "e");
            for (int n = 0; n < 5; n++) {
              sprintf(str, string, arg[n]);
              my_sprintf(str1, string, arg[n]);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
            }
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec e %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "e");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, arg[j]);
      my_sprintf(str1, string, arg[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_e13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec e %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "e");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_e14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, -0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec e %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "e");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, arg[l]);
          my_sprintf(str1, string, arg[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec e %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "e");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, arg[m]);
            my_sprintf(str1, string, arg[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec e %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "e");
            for (int n = 0; n < 5; n++) {
              sprintf(str, string, arg[n]);
              my_sprintf(str1, string, arg[n]);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
            }
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, -1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec e %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "e");
              for (int p = 0; p < 5; p++) {
                sprintf(str, string, arg[p]);
                my_sprintf(str1, string, arg[p]);
                ck_assert_str_eq(str, str1);
                memset(str, '\0', 100);
                memset(str1, '\0', 100);
              }
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_e18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec e %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "e");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_e19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec e %*.*e";
  double arg[5] = {0.0, -0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, 10, 10, arg[i]);
    my_sprintf(str1, string, 10, 10, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_e20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec e %-+ 0#*.*e";
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, 10, 10, arg[i]);
    my_sprintf(str1, string, 10, 10, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_e21) {
  // Flag L
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec e %Le";
  long double arg[5] = {-0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, arg[i]);
    my_sprintf(str1, string, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_E1) {
  // Flag L
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec e %LE";
  long double arg[5] = {-0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, arg[i]);
    my_sprintf(str1, string, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_g1) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec g %g";
  double num[5] = {0.0, 0.00164, -0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, num[i]);
    my_sprintf(str1, string, num[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_g2) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    char string[100] = "Check spec g %";
    strcat(string, flag[i]);
    strcat(string, "g");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, num[j]);
      my_sprintf(str1, string, num[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_g3) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      char string[100] = "Check spec g %";
      strcat(string, flag[i]);
      strcat(string, flag[j]);
      strcat(string, "g");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, num[k]);
        my_sprintf(str1, string, num[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_g4) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        char string[100] = "Check spec g %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, flag[k]);
        strcat(string, "g");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, num[l]);
          my_sprintf(str1, string, num[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g5) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  double num[5] = {0.0, 0.00164, 0.0000164, -1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          char string[100] = "Check spec g %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, flag[l]);
          strcat(string, "g");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, num[m]);
            my_sprintf(str1, string, num[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g6) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec g %-+ 0#g";
  double num[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, num[i]);
    my_sprintf(str1, string, num[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_g7) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec g %";
    strcat(string, num[i]);
    strcat(string, "g");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, arg[j]);
      my_sprintf(str1, string, arg[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_g8) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 7; j++) {
      char string[100] = "Check spec g %";
      strcat(string, flag[i]);
      strcat(string, num[j]);
      strcat(string, "g");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_g9) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, -0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 7; k++) {
        char string[100] = "Check spec g %";
        strcat(string, flag[i]);
        strcat(string, flag[j]);
        strcat(string, num[k]);
        strcat(string, "g");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, arg[l]);
          my_sprintf(str1, string, arg[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g10) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 7; l++) {
          char string[100] = "Check spec g %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, flag[k]);
          strcat(string, num[l]);
          strcat(string, "g");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, arg[m]);
            my_sprintf(str1, string, arg[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g11) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 7; m++) {
            char string[100] = "Check spec g %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, flag[l]);
            strcat(string, num[m]);
            strcat(string, "g");
            for (int n = 0; n < 5; n++) {
              sprintf(str, string, arg[n]);
              my_sprintf(str1, string, arg[n]);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
            }
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g12) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char num[7][3] = {"1", "3", "8", ".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 7; i++) {
    char string[100] = "Check spec g %-+ 0#";
    strcat(string, num[i]);
    strcat(string, "g");
    for (int j = 0; j < 5; j++) {
      sprintf(str, string, arg[j]);
      my_sprintf(str1, string, arg[j]);
      ck_assert_str_eq(str, str1);
      memset(str, '\0', 100);
      memset(str1, '\0', 100);
    }
    memset(string, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_g13) {
  // null flag
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec g %";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "g");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_g14) {
  // 1 flag
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, -0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    for (int j = 0; j < 3; j++) {
      for (int k = 0; k < 4; k++) {
        char string[100] = "Check spec g %";
        strcat(string, flag[i]);
        strcat(string, width[j]);
        strcat(string, precision[k]);
        strcat(string, "g");
        for (int l = 0; l < 5; l++) {
          sprintf(str, string, arg[l]);
          my_sprintf(str1, string, arg[l]);
          ck_assert_str_eq(str, str1);
          memset(str, '\0', 100);
          memset(str1, '\0', 100);
        }
        memset(string, '\0', 100);
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g15) {
  // 2 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 4; i++) {
    for (int j = i + 1; j < 5; j++) {
      for (int k = 0; k < 3; k++) {
        for (int l = 0; l < 4; l++) {
          char string[100] = "Check spec g %";
          strcat(string, flag[i]);
          strcat(string, flag[j]);
          strcat(string, width[k]);
          strcat(string, precision[l]);
          strcat(string, "g");
          for (int m = 0; m < 5; m++) {
            sprintf(str, string, arg[m]);
            my_sprintf(str1, string, arg[m]);
            ck_assert_str_eq(str, str1);
            memset(str, '\0', 100);
            memset(str1, '\0', 100);
          }
          memset(string, '\0', 100);
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g16) {
  // 3 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = i + 1; j < 4; j++) {
      for (int k = j + 1; k < 5; k++) {
        for (int l = 0; l < 3; l++) {
          for (int m = 0; m < 4; m++) {
            char string[100] = "Check spec g %";
            strcat(string, flag[i]);
            strcat(string, flag[j]);
            strcat(string, flag[k]);
            strcat(string, width[l]);
            strcat(string, precision[m]);
            strcat(string, "g");
            for (int n = 0; n < 5; n++) {
              sprintf(str, string, arg[n]);
              my_sprintf(str1, string, arg[n]);
              ck_assert_str_eq(str, str1);
              memset(str, '\0', 100);
              memset(str1, '\0', 100);
            }
            memset(string, '\0', 100);
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g17) {
  // 4 flags
  char str[100] = {0};
  char str1[100] = {0};
  char flag[5][2] = {"-", "+", " ", "0", "#"};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, -1567.123, 123456789.123456};
  for (int i = 0; i < 2; i++) {
    for (int j = i + 1; j < 3; j++) {
      for (int k = j + 1; k < 4; k++) {
        for (int l = k + 1; l < 5; l++) {
          for (int m = 0; m < 3; m++) {
            for (int n = 0; n < 4; n++) {
              char string[100] = "Check spec g %";
              strcat(string, flag[i]);
              strcat(string, flag[j]);
              strcat(string, flag[k]);
              strcat(string, flag[l]);
              strcat(string, width[m]);
              strcat(string, precision[n]);
              strcat(string, "g");
              for (int p = 0; p < 5; p++) {
                sprintf(str, string, arg[p]);
                my_sprintf(str1, string, arg[p]);
                ck_assert_str_eq(str, str1);
                memset(str, '\0', 100);
                memset(str1, '\0', 100);
              }
              memset(string, '\0', 100);
            }
          }
        }
      }
    }
  }
}
END_TEST

START_TEST(sprintf_g18) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char width[3][2] = {"1", "3", "8"};
  char precision[4][3] = {".", ".1", ".3", ".8"};
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 4; j++) {
      char string[100] = "Check spec g %-+ 0#";
      strcat(string, width[i]);
      strcat(string, precision[j]);
      strcat(string, "g");
      for (int k = 0; k < 5; k++) {
        sprintf(str, string, arg[k]);
        my_sprintf(str1, string, arg[k]);
        ck_assert_str_eq(str, str1);
        memset(str, '\0', 100);
        memset(str1, '\0', 100);
      }
      memset(string, '\0', 100);
    }
  }
}
END_TEST

START_TEST(sprintf_g19) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec g %*.*g";
  double arg[5] = {0.0, -0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, 10, 10, arg[i]);
    my_sprintf(str1, string, 10, 10, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_g20) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec g %-+ 0#*.*g";
  double arg[5] = {0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, 10, 10, arg[i]);
    my_sprintf(str1, string, 10, 10, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_g21) {
  // All flags
  char str[100] = "hello";
  char *format = my_NULL;
  ck_assert_int_eq(my_sprintf(str, format, 10), -1);
}
END_TEST

START_TEST(sprintf_G1) {
  // All flags
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec G %-+ 0#.3G";
  double arg = 10.23;
  sprintf(str, string, 10, 10, arg);
  my_sprintf(str1, string, 10, 10, arg);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_g22) {
  // Flag L
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec g %Lg";
  long double arg[5] = {-0.0, 0.00164, 0.0000164, 1567.123, 123456789.123456};
  for (int i = 0; i < 5; i++) {
    sprintf(str, string, arg[i]);
    my_sprintf(str1, string, arg[i]);
    ck_assert_str_eq(str, str1);
    memset(str, '\0', 100);
    memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(sprintf_c1) {
  // simple char
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %c and %c";
  char sym = 'l';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_c2) {
  // empty char
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %c and %c";
  char sym = '\0';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_c3) {
  // flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %-c and %-c";
  char sym = 'l';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_c4) {
  // width
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %12c and %-12c";
  char sym = 'l';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_c5) {
  // precision
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %.12c and %-.12c";
  char sym = 'l';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_c6) {
  // precision
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %12.12c and % -+#0c";
  char sym = 'l';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_c7) {
  // precision
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %12.10c and %10.0c";
  char sym = 'l';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST
START_TEST(sprintf_c8) {
  // precision
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %12.c and %-10.c";
  char sym = '\0';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_c9) {
  // precision
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %12.c and %-10.c";
  char *sym = NULL;
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_c10) {
  // precision
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %12.lc and %-10.hc";
  char sym = '\t';
  char sym1 = '%';
  ck_assert_int_eq(sprintf(str, string, sym, sym1),
                   my_sprintf(str1, string, sym, sym1));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s1) {
  // simple src
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %s";
  char src[100] = "hi, Mag";
  sprintf(str, string, src);
  my_sprintf(str1, string, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s2) {
  // flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %-s";
  char src[100] = "hi, Mag";
  sprintf(str, string, src);
  my_sprintf(str1, string, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s3) {
  // width
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %12s";
  char src[100] = "hello, my friends";
  sprintf(str, string, src);
  my_sprintf(str1, string, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s4) {
  // precision
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %.12s";
  char src[100] = "hello, my friends";
  sprintf(str, string, src);
  my_sprintf(str1, string, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s5) {
  // width and precision
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %8.12s %12.8s";
  char src[100] = "hello, my friends";
  sprintf(str, string, src, src);
  my_sprintf(str1, string, src, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s6) {
  // width with flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %-12s";
  char src[100] = "hello, my friends";
  sprintf(str, string, src);
  my_sprintf(str1, string, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s7) {
  // precision with flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %-.12s";
  char src[100] = "hello, my friends";
  sprintf(str, string, src);
  my_sprintf(str1, string, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s8) {
  // precision and width with flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %-10.12s and %-12.10s and %-12.12s";
  char src[100] = "hello, my friends";
  sprintf(str, string, src, src, src);
  my_sprintf(str1, string, src, src, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s9) {
  // empty src
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %8.12s %12.8s %-12s and %-.12s and %-10.12s";
  char src[100] = "\0";
  sprintf(str, string, src, src, src, src, src);
  my_sprintf(str1, string, src, src, src, src, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s10) {
  // src = NULL
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %s";
  char *src = my_NULL;
  sprintf(str, string, src);
  my_sprintf(str1, string, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s11) {
  // precision and width with flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %-10.12s and %-12.10s and %-12.12s";
  char *src = my_NULL;
  sprintf(str, string, src, src, src);
  my_sprintf(str1, string, src, src, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_s12) {
  // precision and width with flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %10.12s and %12.10s and %12.12s";
  char *src = my_NULL;
  sprintf(str, string, src, src, src);
  my_sprintf(str1, string, src, src, src);
  ck_assert_str_eq(str, str1);
}
END_TEST
START_TEST(sprintf_s13) {
  // precision and width with flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec c %10.12s and %12.10s and %12.12s";
  char src[100] = "hello, my friends";
  sprintf(str, string, src, src, src);
  my_sprintf(str1, string, src, src, src);
  ck_assert_str_eq(str, str1);
}
END_TEST
START_TEST(sprintf_s14) {
  // precision and width with flag -
  char str[100] = {0};
  char str1[100] = {0};
  char string[100] = "Check spec s %10.12hs and %hs and %.10hs";
  char src[100] = "hello, my friends";
  sprintf(str, string, src, src, src);
  my_sprintf(str1, string, src, src, src);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(sprintf_big) {
  // precision and width with flag -
  char str[1000] = {0};
  char str1[1000] = {0};
  char string[1000] =
      "Check spec % -+0#5.10c and % -+0#5.10s and % -+0#5.10ld% -+0#5.10li % "
      "-+0#5.10lf % -+0#5.10lu % -+0#5.10l%";
  char src[100] = "hello, my friends";
  ck_assert_int_eq(sprintf(str, string, 'l', src, INT32_MAX, INT32_MIN,
                           1.234567892, UINT32_MAX),
                   my_sprintf(str1, string, 'l', src, INT32_MAX, INT32_MIN,
                               1.234567892, UINT32_MAX));
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(my_strncat_1) {
  // full str
  char str[100] = "Hello, ";
  char str1[100] = "Hello, ";
  char string[100] = "my friends";
  strncat(str, string, 10);
  my_strncat(str1, string, 10);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(my_strncat_2) {
  // some str
  char str[100] = "Hello, ";
  char str1[100] = "Hello, ";
  char string[100] = "my friends";
  strncat(str, string, 5);
  my_strncat(str1, string, 5);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(my_strncat_3) {
  // more str
  char str[100] = "Hello, ";
  char str1[100] = "Hello, ";
  char string[100] = "my friends";
  strncat(str, string, 30);
  my_strncat(str1, string, 30);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(my_strncat_4) {
  // more str
  char str[10] = "Hello, ";
  char str1[10] = "Hello, ";
  char string[100] = "my friends";
  strncat(str, string, 2);
  my_strncat(str1, string, 2);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(my_strncat_5) {
  // empty str
  char str[10] = "Hello, ";
  char str1[10] = "Hello, ";
  char string[100] = "\0";
  strncat(str, string, 2);
  my_strncat(str1, string, 2);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(my_strncat_6) {
  // empty str
  char str[2] = "\0";
  char str1[2] = "\0";
  char string[1] = "\0";
  strncat(str, string, 1);
  my_strncat(str1, string, 1);
  ck_assert_str_eq(str, str1);
}
END_TEST

START_TEST(my_strerror_1) {
  // empty str
  char str[100] = "\0";
  char str1[100] = "\0";
  for (int numerror = -10; numerror < 330; numerror++) {
    //  my_strcpy(str, strerror(numerror));
    //  my_strcpy(str1, my_strerror(numerror));
    ck_assert_str_eq(strerror(numerror), my_strerror(numerror));
    //  memset(str, '\0', 100);
    //  memset(str1, '\0', 100);
  }
}
END_TEST

START_TEST(my_strtok_1) {
  // empty str
  char str[100] = "test1/test2/test3/test4";
  char str1[100] = "test1/test2/test3/test4";
  char sep[10] = "/";
  char *istr = strtok(str, sep);
  char *istr1 = my_strtok(str1, sep);
  ck_assert_str_eq(istr, istr1);
  while (istr != NULL) {
    istr = strtok(NULL, sep);
    istr1 = my_strtok(my_NULL, sep);
    if (istr != NULL) {
      ck_assert_str_eq(istr, istr1);
    } else {
      ck_assert_ptr_null(istr1);
    }
  }
}
END_TEST

START_TEST(my_strtok_2) {
  // empty str
  char str[100] = "test1/test2/test3/test4";
  char str1[100] = "test1/test2/test3/test4";
  char sep[10] = "/te";
  char *istr = strtok(str, sep);
  char *istr1 = my_strtok(str1, sep);
  ck_assert_str_eq(istr, istr1);
  while (istr != NULL) {
    istr = strtok(NULL, sep);
    istr1 = my_strtok(my_NULL, sep);
    if (istr != NULL) {
      ck_assert_str_eq(istr, istr1);
    } else {
      ck_assert_ptr_null(istr1);
    }
  }
}
END_TEST

START_TEST(my_strtok_3) {
  // empty str
  char str[100] = "test1/test2/test3/test4";
  char str1[100] = "test1/test2/test3/test4";
  char sep[10] = "f";
  char *istr = strtok(str, sep);
  char *istr1 = my_strtok(str1, sep);
  ck_assert_str_eq(istr, istr1);
  while (istr != NULL) {
    istr = strtok(NULL, sep);
    istr1 = my_strtok(my_NULL, sep);
    ck_assert_ptr_null(istr1);
  }
}
END_TEST

START_TEST(my_strtok_4) {
  // empty str
  char str[100] = "\0";
  char str1[100] = "\0";
  char sep[10] = "sep";
  char *istr = strtok(str, sep);
  char *istr1 = strtok(str1, sep);
  ck_assert_ptr_null(istr1);
}
END_TEST

START_TEST(my_to_upper_1) {
  // simple str
  char str[100] = "Hello";
  char check_str[100] = "HELLO";
  char *ptr = my_to_upper(str);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_to_upper_2) {
  // simple str
  char str[100] = "HELLO";
  char check_str[100] = "HELLO";
  char *ptr = my_to_upper(str);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_to_upper_3) {
  // empty str
  char str[100] = "\0";
  char check_str[100] = "\0";
  char *ptr = my_to_upper(str);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_to_upper_4) {
  // empty str
  char str[100] = "Hello 123, @%()-=";
  char check_str[100] = "HELLO 123, @%()-=";
  char *ptr = my_to_upper(str);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_to_upper_5) {
  // str = NULL
  char *str = my_NULL;
  // char check_str[100] = "HELLO 123, @%()-=";
  char *ptr = my_to_upper(str);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_to_lower_1) {
  // simple str
  char str[100] = "HELLO";
  char check_str[100] = "hello";
  char *ptr = my_to_lower(str);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_to_lower_2) {
  // simple str
  char str[100] = "hello";
  char check_str[100] = "hello";
  char *ptr = my_to_lower(str);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_to_lower_3) {
  // empty str
  char str[100] = "\0";
  char check_str[100] = "\0";
  char *ptr = my_to_lower(str);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_to_lower_4) {
  // empty str
  char str[100] = "HELLO 123, $%()-=";
  char check_str[100] = "hello 123, $%()-=";
  char *ptr = my_to_lower(str);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_to_lower_5) {
  // str = NULL
  char *str = my_NULL;
  char check_str[100] = "hello 123, $%()-=";
  char *ptr = my_to_lower(str);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_insert_1) {
  // full str
  char src[100] = "hello";
  char str[100] = "someone and you";
  my_size_t index = 3;
  char check_str[100] = "helsomeone and youlo";
  char *ptr = my_insert(src, str, index);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_insert_2) {
  // full str
  char src[100] = "hello";
  char str[100] = "someone and you";
  my_size_t index = 0;
  char check_str[100] = "someone and youhello";
  char *ptr = my_insert(src, str, index);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_insert_3) {
  // index > len
  char src[100] = "hello";
  char str[100] = "someone and you";
  my_size_t index = 8;
  char *ptr = my_insert(src, str, index);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_insert_4) {
  // index < 0
  char src[100] = "hello";
  char str[100] = "someone and you";
  my_size_t index = -10;
  char *ptr = my_insert(src, str, index);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_insert_5) {
  // empty src
  char src[100] = "\0";
  char str[100] = "someone and you";
  my_size_t index = 1;
  char *ptr = my_insert(src, str, index);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_insert_6) {
  // empty str
  char src[100] = "hello";
  char str[100] = "\0";
  my_size_t index = 1;
  char check_str[100] = "hello";
  char *ptr = my_insert(src, str, index);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_insert_7) {
  // empty str, src
  char src[100] = "\0";
  char str[100] = "\0";
  my_size_t index = 0;
  char check_str[100] = "\0";
  char *ptr = my_insert(src, str, index);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_insert_8) {
  // src = NULL
  char *src = my_NULL;
  char str[100] = "\0";
  my_size_t index = 0;
  char check_str[100] = "\0";
  char *ptr = my_insert(src, str, index);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_insert_9) {
  // str = NULL
  char src[100] = "Hello";
  char *str = my_NULL;
  my_size_t index = 0;
  char check_str[100] = "\0";
  char *ptr = my_insert(src, str, index);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_insert_10) {
  // str, src = NULL
  char *src = my_NULL;
  char *str = my_NULL;
  my_size_t index = 0;
  char check_str[100] = "\0";
  char *ptr = my_insert(src, str, index);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_trim_1) {
  // full str
  char src[100] = "hello";
  char trim_chars[100] = "h";
  char check_str[100] = "ello";
  char *ptr = my_trim(src, trim_chars);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_trim_2) {
  // full str
  char src[100] = "hello";
  char trim_chars[100] = "heo";
  char check_str[100] = "ll";
  char *ptr = my_trim(src, trim_chars);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_trim_3) {
  // full str
  char src[100] = "hello, world";
  char trim_chars[100] = "l";
  char check_str[100] = "hello, world";
  char *ptr = my_trim(src, trim_chars);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_trim_4) {
  // full str
  char src[100] = "hellohello";
  char trim_chars[100] = "hello";
  char check_str[100] = "\0";
  char *ptr = my_trim(src, trim_chars);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_trim_5) {
  // empty str
  char src[100] = "hello";
  char trim_chars[100] = "\0";
  char check_str[100] = "hello";
  char *ptr = my_trim(src, trim_chars);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_trim_6) {
  // empty src
  char src[100] = "\0";
  char trim_chars[100] = "hell";
  char check_str[100] = "\0";
  char *ptr = my_trim(src, trim_chars);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_trim_7) {
  // empty src
  char *src = my_NULL;
  char trim_chars[100] = "hell";
  char *ptr = my_trim(src, trim_chars);
  ck_assert_ptr_null(ptr);
}
END_TEST

START_TEST(my_trim_8) {
  // empty trim
  char src[100] = "hello";
  char *trim_chars = my_NULL;
  char check_str[100] = "hello";
  char *ptr = my_trim(src, trim_chars);
  ck_assert_str_eq(check_str, ptr);
  free(ptr);
}
END_TEST

START_TEST(my_trim_9) {
  // empty src, trim
  char *src = my_NULL;
  char *trim_chars = my_NULL;
  char *ptr = my_trim(src, trim_chars);
  ck_assert_ptr_null(ptr);
}
END_TEST

int main(void) {
  Suite *s1 = suite_create("Core");
  TCase *tc1_1 = tcase_create("Core");
  SRunner *sr = srunner_create(s1);
  int nf;

  suite_add_tcase(s1, tc1_1);
  tcase_add_test(tc1_1, memcmp1);
  tcase_add_test(tc1_1, memcmp2);
  tcase_add_test(tc1_1, memcmp3);
  tcase_add_test(tc1_1, memcmp4);
  tcase_add_test(tc1_1, memcmp5);
  tcase_add_test(tc1_1, memcmp6);
  tcase_add_test(tc1_1, memcmp7);
  tcase_add_test(tc1_1, strcmp1);
  tcase_add_test(tc1_1, strcmp2);
  tcase_add_test(tc1_1, strcmp3);
  tcase_add_test(tc1_1, strcmp4);
  tcase_add_test(tc1_1, strcmp5);
  tcase_add_test(tc1_1, strcmp6);
  tcase_add_test(tc1_1, strncmp1);
  tcase_add_test(tc1_1, strncmp2);
  tcase_add_test(tc1_1, strncmp3);
  tcase_add_test(tc1_1, strncmp4);
  tcase_add_test(tc1_1, strncmp5);
  tcase_add_test(tc1_1, strncmp6);
  tcase_add_test(tc1_1, strncmp7);
  tcase_add_test(tc1_1, memcpy1);
  tcase_add_test(tc1_1, memcpy2);
  tcase_add_test(tc1_1, memcpy3);
  tcase_add_test(tc1_1, memcpy4);
  tcase_add_test(tc1_1, memcpy5);
  tcase_add_test(tc1_1, memcpy6);
  tcase_add_test(tc1_1, memcpy7);
  tcase_add_test(tc1_1, memcpy8);
  tcase_add_test(tc1_1, memcpy9);
  tcase_add_test(tc1_1, memcpy10);
  tcase_add_test(tc1_1, memcpy11);
  tcase_add_test(tc1_1, memcpy12);
  tcase_add_test(tc1_1, memcpy13);
  tcase_add_test(tc1_1, memmove1);
  tcase_add_test(tc1_1, memmove2);
  tcase_add_test(tc1_1, memmove3);
  tcase_add_test(tc1_1, memmove4);
  tcase_add_test(tc1_1, memmove5);
  tcase_add_test(tc1_1, memmove6);
  tcase_add_test(tc1_1, memmove7);
  tcase_add_test(tc1_1, memmove8);
  tcase_add_test(tc1_1, memmove9);
  tcase_add_test(tc1_1, memmove10);
  tcase_add_test(tc1_1, memmove11);
  tcase_add_test(tc1_1, memmove12);
  tcase_add_test(tc1_1, memmove13);
  tcase_add_test(tc1_1, memset1);
  tcase_add_test(tc1_1, memset2);
  tcase_add_test(tc1_1, memset3);
  tcase_add_test(tc1_1, memset4);
  tcase_add_test(tc1_1, strcpy1);
  tcase_add_test(tc1_1, strcpy2);
  tcase_add_test(tc1_1, strcpy3);
  tcase_add_test(tc1_1, strcpy4);
  tcase_add_test(tc1_1, strncpy3);
  tcase_add_test(tc1_1, strncpy4);
  tcase_add_test(tc1_1, strncpy5);
  tcase_add_test(tc1_1, strncpy7);
  tcase_add_test(tc1_1, strlen1);
  tcase_add_test(tc1_1, strlen2);
  tcase_add_test(tc1_1, strlen3);
  tcase_add_test(tc1_1, strspn1);
  tcase_add_test(tc1_1, strspn2);
  tcase_add_test(tc1_1, strspn3);
  tcase_add_test(tc1_1, strspn4);
  tcase_add_test(tc1_1, strspn5);
  tcase_add_test(tc1_1, strspn6);
  tcase_add_test(tc1_1, strspn7);
  tcase_add_test(tc1_1, strspn8);
  tcase_add_test(tc1_1, strspn9);
  tcase_add_test(tc1_1, strspn10);
  tcase_add_test(tc1_1, strspn11);
  tcase_add_test(tc1_1, strcspn1);
  tcase_add_test(tc1_1, strcspn2);
  tcase_add_test(tc1_1, strcspn3);
  tcase_add_test(tc1_1, strcspn4);
  tcase_add_test(tc1_1, strcspn5);
  tcase_add_test(tc1_1, strcspn6);
  tcase_add_test(tc1_1, strcspn7);
  tcase_add_test(tc1_1, strcspn8);
  tcase_add_test(tc1_1, strcspn9);
  tcase_add_test(tc1_1, strcspn10);
  tcase_add_test(tc1_1, strcspn11);
  tcase_add_test(tc1_1, memchr1);
  tcase_add_test(tc1_1, memchr2);
  tcase_add_test(tc1_1, memchr3);
  tcase_add_test(tc1_1, memchr4);
  tcase_add_test(tc1_1, memchr5);
  tcase_add_test(tc1_1, memchr6);
  tcase_add_test(tc1_1, memchr7);
  tcase_add_test(tc1_1, memchr8);
  tcase_add_test(tc1_1, memchr9);
  tcase_add_test(tc1_1, memchr10);
  tcase_add_test(tc1_1, memchr11);
  tcase_add_test(tc1_1, strchr1);
  tcase_add_test(tc1_1, strchr2);
  tcase_add_test(tc1_1, strchr3);
  tcase_add_test(tc1_1, strchr4);
  tcase_add_test(tc1_1, strchr5);
  tcase_add_test(tc1_1, strchr6);
  tcase_add_test(tc1_1, strchr7);
  tcase_add_test(tc1_1, strchr8);
  tcase_add_test(tc1_1, strchr9);
  tcase_add_test(tc1_1, strstr1);
  tcase_add_test(tc1_1, strstr2);
  tcase_add_test(tc1_1, strstr3);
  tcase_add_test(tc1_1, strstr4);
  tcase_add_test(tc1_1, strstr5);
  tcase_add_test(tc1_1, strstr6);
  tcase_add_test(tc1_1, strstr7);
  tcase_add_test(tc1_1, strstr8);
  tcase_add_test(tc1_1, strstr9);
  tcase_add_test(tc1_1, strrchr1);
  tcase_add_test(tc1_1, strrchr2);
  tcase_add_test(tc1_1, strrchr3);
  tcase_add_test(tc1_1, strrchr4);
  tcase_add_test(tc1_1, strrchr5);
  tcase_add_test(tc1_1, strrchr6);
  tcase_add_test(tc1_1, strpbrk1);
  tcase_add_test(tc1_1, strpbrk2);
  tcase_add_test(tc1_1, strpbrk3);
  tcase_add_test(tc1_1, strpbrk4);
  tcase_add_test(tc1_1, strpbrk5);
  tcase_add_test(tc1_1, strpbrk6);
  tcase_add_test(tc1_1, sprintf_procent1);
  tcase_add_test(tc1_1, sprintf_procent2);
  tcase_add_test(tc1_1, sprintf_procent3);
  tcase_add_test(tc1_1, sprintf_procent4);
  tcase_add_test(tc1_1, sprintf_d1);
  tcase_add_test(tc1_1, sprintf_d2);
  tcase_add_test(tc1_1, sprintf_d3);
  tcase_add_test(tc1_1, sprintf_d4);
  tcase_add_test(tc1_1, sprintf_d5);
  tcase_add_test(tc1_1, sprintf_d6);
  tcase_add_test(tc1_1, sprintf_d7);
  tcase_add_test(tc1_1, sprintf_d8);
  tcase_add_test(tc1_1, sprintf_d9);
  tcase_add_test(tc1_1, sprintf_d10);
  tcase_add_test(tc1_1, sprintf_d11);
  tcase_add_test(tc1_1, sprintf_d12);
  tcase_add_test(tc1_1, sprintf_d13);
  tcase_add_test(tc1_1, sprintf_d14);
  tcase_add_test(tc1_1, sprintf_d15);
  tcase_add_test(tc1_1, sprintf_d16);
  tcase_add_test(tc1_1, sprintf_d17);
  tcase_add_test(tc1_1, sprintf_d18);
  tcase_add_test(tc1_1, sprintf_d19);
  tcase_add_test(tc1_1, sprintf_d20);
  tcase_add_test(tc1_1, sprintf_d21);
  tcase_add_test(tc1_1, sprintf_d22);
  tcase_add_test(tc1_1, sprintf_d23);
  tcase_add_test(tc1_1, sprintf_i1);
  tcase_add_test(tc1_1, sprintf_i2);
  tcase_add_test(tc1_1, sprintf_i3);
  tcase_add_test(tc1_1, sprintf_i4);
  tcase_add_test(tc1_1, sprintf_i5);
  tcase_add_test(tc1_1, sprintf_i6);
  tcase_add_test(tc1_1, sprintf_i7);
  tcase_add_test(tc1_1, sprintf_i8);
  tcase_add_test(tc1_1, sprintf_i9);
  tcase_add_test(tc1_1, sprintf_i10);
  tcase_add_test(tc1_1, sprintf_i11);
  tcase_add_test(tc1_1, sprintf_i12);
  tcase_add_test(tc1_1, sprintf_i13);
  tcase_add_test(tc1_1, sprintf_i14);
  tcase_add_test(tc1_1, sprintf_i15);
  tcase_add_test(tc1_1, sprintf_i16);
  tcase_add_test(tc1_1, sprintf_i17);
  tcase_add_test(tc1_1, sprintf_i18);
  tcase_add_test(tc1_1, sprintf_i19);
  tcase_add_test(tc1_1, sprintf_i20);
  tcase_add_test(tc1_1, sprintf_i21);
  tcase_add_test(tc1_1, sprintf_i22);
  tcase_add_test(tc1_1, sprintf_x1);
  tcase_add_test(tc1_1, sprintf_x2);
  tcase_add_test(tc1_1, sprintf_x3);
  tcase_add_test(tc1_1, sprintf_x4);
  tcase_add_test(tc1_1, sprintf_x5);
  tcase_add_test(tc1_1, sprintf_x6);
  tcase_add_test(tc1_1, sprintf_x7);
  tcase_add_test(tc1_1, sprintf_x8);
  tcase_add_test(tc1_1, sprintf_x9);
  tcase_add_test(tc1_1, sprintf_x10);
  tcase_add_test(tc1_1, sprintf_x11);
  tcase_add_test(tc1_1, sprintf_x12);
  tcase_add_test(tc1_1, sprintf_x13);
  tcase_add_test(tc1_1, sprintf_x14);
  tcase_add_test(tc1_1, sprintf_x15);
  tcase_add_test(tc1_1, sprintf_x16);
  tcase_add_test(tc1_1, sprintf_x17);
  tcase_add_test(tc1_1, sprintf_x18);
  tcase_add_test(tc1_1, sprintf_x19);
  tcase_add_test(tc1_1, sprintf_x20);
  tcase_add_test(tc1_1, sprintf_x21);
  tcase_add_test(tc1_1, sprintf_X1);
  tcase_add_test(tc1_1, sprintf_X2);
  tcase_add_test(tc1_1, sprintf_X3);
  tcase_add_test(tc1_1, sprintf_X4);
  tcase_add_test(tc1_1, sprintf_X5);
  tcase_add_test(tc1_1, sprintf_X6);
  tcase_add_test(tc1_1, sprintf_X7);
  tcase_add_test(tc1_1, sprintf_X8);
  tcase_add_test(tc1_1, sprintf_X9);
  tcase_add_test(tc1_1, sprintf_X10);
  tcase_add_test(tc1_1, sprintf_X11);
  tcase_add_test(tc1_1, sprintf_X12);
  tcase_add_test(tc1_1, sprintf_X13);
  tcase_add_test(tc1_1, sprintf_X14);
  tcase_add_test(tc1_1, sprintf_X15);
  tcase_add_test(tc1_1, sprintf_X16);
  tcase_add_test(tc1_1, sprintf_X17);
  tcase_add_test(tc1_1, sprintf_X18);
  tcase_add_test(tc1_1, sprintf_X19);
  tcase_add_test(tc1_1, sprintf_X20);
  tcase_add_test(tc1_1, sprintf_X21);
  tcase_add_test(tc1_1, sprintf_u1);
  tcase_add_test(tc1_1, sprintf_u2);
  tcase_add_test(tc1_1, sprintf_u3);
  tcase_add_test(tc1_1, sprintf_u4);
  tcase_add_test(tc1_1, sprintf_u5);
  tcase_add_test(tc1_1, sprintf_u6);
  tcase_add_test(tc1_1, sprintf_u7);
  tcase_add_test(tc1_1, sprintf_u8);
  tcase_add_test(tc1_1, sprintf_u9);
  tcase_add_test(tc1_1, sprintf_u10);
  tcase_add_test(tc1_1, sprintf_u11);
  tcase_add_test(tc1_1, sprintf_u12);
  tcase_add_test(tc1_1, sprintf_u13);
  tcase_add_test(tc1_1, sprintf_u14);
  tcase_add_test(tc1_1, sprintf_u15);
  tcase_add_test(tc1_1, sprintf_u16);
  tcase_add_test(tc1_1, sprintf_u17);
  tcase_add_test(tc1_1, sprintf_u18);
  tcase_add_test(tc1_1, sprintf_u19);
  tcase_add_test(tc1_1, sprintf_u20);
  tcase_add_test(tc1_1, sprintf_u21);
  tcase_add_test(tc1_1, sprintf_u22);
  tcase_add_test(tc1_1, sprintf_o1);
  tcase_add_test(tc1_1, sprintf_o2);
  tcase_add_test(tc1_1, sprintf_o3);
  tcase_add_test(tc1_1, sprintf_o4);
  tcase_add_test(tc1_1, sprintf_o5);
  tcase_add_test(tc1_1, sprintf_o6);
  tcase_add_test(tc1_1, sprintf_o7);
  tcase_add_test(tc1_1, sprintf_o8);
  tcase_add_test(tc1_1, sprintf_o9);
  tcase_add_test(tc1_1, sprintf_o10);
  tcase_add_test(tc1_1, sprintf_o11);
  tcase_add_test(tc1_1, sprintf_o12);
  tcase_add_test(tc1_1, sprintf_o13);
  tcase_add_test(tc1_1, sprintf_o14);
  tcase_add_test(tc1_1, sprintf_o15);
  tcase_add_test(tc1_1, sprintf_o16);
  tcase_add_test(tc1_1, sprintf_o17);
  tcase_add_test(tc1_1, sprintf_o18);
  tcase_add_test(tc1_1, sprintf_o19);
  tcase_add_test(tc1_1, sprintf_o20);
  tcase_add_test(tc1_1, sprintf_o21);
  tcase_add_test(tc1_1, sprintf_p1);
  tcase_add_test(tc1_1, sprintf_p2);
  tcase_add_test(tc1_1, sprintf_p3);
  tcase_add_test(tc1_1, sprintf_p4);
  tcase_add_test(tc1_1, sprintf_p5);
  tcase_add_test(tc1_1, sprintf_p6);
  tcase_add_test(tc1_1, sprintf_p7);
  tcase_add_test(tc1_1, sprintf_p8);
  tcase_add_test(tc1_1, sprintf_p9);
  tcase_add_test(tc1_1, sprintf_p10);
  tcase_add_test(tc1_1, sprintf_p11);
  tcase_add_test(tc1_1, sprintf_p12);
  tcase_add_test(tc1_1, sprintf_p13);
  tcase_add_test(tc1_1, sprintf_p14);
  tcase_add_test(tc1_1, sprintf_p15);
  tcase_add_test(tc1_1, sprintf_p16);
  tcase_add_test(tc1_1, sprintf_p17);
  tcase_add_test(tc1_1, sprintf_p18);
  tcase_add_test(tc1_1, sprintf_p19);
  tcase_add_test(tc1_1, sprintf_p20);
  tcase_add_test(tc1_1, sprintf_p21);
  tcase_add_test(tc1_1, sprintf_p22);
  tcase_add_test(tc1_1, sprintf_procent_1);
  tcase_add_test(tc1_1, sprintf_n);
  tcase_add_test(tc1_1, sprintf_f1);
  tcase_add_test(tc1_1, sprintf_f2);
  tcase_add_test(tc1_1, sprintf_f3);
  tcase_add_test(tc1_1, sprintf_f4);
  tcase_add_test(tc1_1, sprintf_f5);
  tcase_add_test(tc1_1, sprintf_f6);
  tcase_add_test(tc1_1, sprintf_f7);
  tcase_add_test(tc1_1, sprintf_f8);
  tcase_add_test(tc1_1, sprintf_f9);
  tcase_add_test(tc1_1, sprintf_f10);
  tcase_add_test(tc1_1, sprintf_f11);
  tcase_add_test(tc1_1, sprintf_f12);
  tcase_add_test(tc1_1, sprintf_f13);
  tcase_add_test(tc1_1, sprintf_f14);
  tcase_add_test(tc1_1, sprintf_f15);
  tcase_add_test(tc1_1, sprintf_f16);
  tcase_add_test(tc1_1, sprintf_f17);
  tcase_add_test(tc1_1, sprintf_f18);
  tcase_add_test(tc1_1, sprintf_f19);
  tcase_add_test(tc1_1, sprintf_f20);
  tcase_add_test(tc1_1, sprintf_f21);
  tcase_add_test(tc1_1, sprintf_e1);
  tcase_add_test(tc1_1, sprintf_e2);
  tcase_add_test(tc1_1, sprintf_e3);
  tcase_add_test(tc1_1, sprintf_e4);
  tcase_add_test(tc1_1, sprintf_e5);
  tcase_add_test(tc1_1, sprintf_e6);
  tcase_add_test(tc1_1, sprintf_e7);
  tcase_add_test(tc1_1, sprintf_e8);
  tcase_add_test(tc1_1, sprintf_e9);
  tcase_add_test(tc1_1, sprintf_e10);
  tcase_add_test(tc1_1, sprintf_e11);
  tcase_add_test(tc1_1, sprintf_e12);
  tcase_add_test(tc1_1, sprintf_e13);
  tcase_add_test(tc1_1, sprintf_e14);
  tcase_add_test(tc1_1, sprintf_e15);
  tcase_add_test(tc1_1, sprintf_e16);
  tcase_add_test(tc1_1, sprintf_e17);
  tcase_add_test(tc1_1, sprintf_e18);
  tcase_add_test(tc1_1, sprintf_e19);
  tcase_add_test(tc1_1, sprintf_e20);
  tcase_add_test(tc1_1, sprintf_e21);
  tcase_add_test(tc1_1, sprintf_E1);
  tcase_add_test(tc1_1, sprintf_g1);
  tcase_add_test(tc1_1, sprintf_g2);
  tcase_add_test(tc1_1, sprintf_g3);
  tcase_add_test(tc1_1, sprintf_g4);
  tcase_add_test(tc1_1, sprintf_g5);
  tcase_add_test(tc1_1, sprintf_g6);
  tcase_add_test(tc1_1, sprintf_g7);
  tcase_add_test(tc1_1, sprintf_g8);
  tcase_add_test(tc1_1, sprintf_g9);
  tcase_add_test(tc1_1, sprintf_g10);
  tcase_add_test(tc1_1, sprintf_g11);
  tcase_add_test(tc1_1, sprintf_g12);
  tcase_add_test(tc1_1, sprintf_g13);
  tcase_add_test(tc1_1, sprintf_g14);
  tcase_add_test(tc1_1, sprintf_g15);
  tcase_add_test(tc1_1, sprintf_g16);
  tcase_add_test(tc1_1, sprintf_g17);
  tcase_add_test(tc1_1, sprintf_g18);
  tcase_add_test(tc1_1, sprintf_g19);
  tcase_add_test(tc1_1, sprintf_g20);
  tcase_add_test(tc1_1, sprintf_g21);
  tcase_add_test(tc1_1, sprintf_g22);
  tcase_add_test(tc1_1, sprintf_G1);
  tcase_add_test(tc1_1, sprintf_c1);
  tcase_add_test(tc1_1, sprintf_c2);
  tcase_add_test(tc1_1, sprintf_c3);
  tcase_add_test(tc1_1, sprintf_c4);
  tcase_add_test(tc1_1, sprintf_c5);
  tcase_add_test(tc1_1, sprintf_c6);
  tcase_add_test(tc1_1, sprintf_c7);
  tcase_add_test(tc1_1, sprintf_c8);
  tcase_add_test(tc1_1, sprintf_c9);
  tcase_add_test(tc1_1, sprintf_c10);
  tcase_add_test(tc1_1, sprintf_s1);
  tcase_add_test(tc1_1, sprintf_s2);
  tcase_add_test(tc1_1, sprintf_s3);
  tcase_add_test(tc1_1, sprintf_s4);
  tcase_add_test(tc1_1, sprintf_s5);
  tcase_add_test(tc1_1, sprintf_s6);
  tcase_add_test(tc1_1, sprintf_s7);
  tcase_add_test(tc1_1, sprintf_s8);
  tcase_add_test(tc1_1, sprintf_s9);
  tcase_add_test(tc1_1, sprintf_s10);
  tcase_add_test(tc1_1, sprintf_s11);
  tcase_add_test(tc1_1, sprintf_s12);
  tcase_add_test(tc1_1, sprintf_s13);
  tcase_add_test(tc1_1, sprintf_s14);
  tcase_add_test(tc1_1, sprintf_big);
  tcase_add_test(tc1_1, my_strncat_1);
  tcase_add_test(tc1_1, my_strncat_2);
  tcase_add_test(tc1_1, my_strncat_3);
  tcase_add_test(tc1_1, my_strncat_4);
  tcase_add_test(tc1_1, my_strncat_5);
  tcase_add_test(tc1_1, my_strncat_6);
  tcase_add_test(tc1_1, my_strerror_1);
  tcase_add_test(tc1_1, my_strtok_1);
  tcase_add_test(tc1_1, my_strtok_2);
  tcase_add_test(tc1_1, my_strtok_3);
  tcase_add_test(tc1_1, my_strtok_4);
  tcase_add_test(tc1_1, my_to_upper_1);
  tcase_add_test(tc1_1, my_to_upper_2);
  tcase_add_test(tc1_1, my_to_upper_3);
  tcase_add_test(tc1_1, my_to_upper_4);
  tcase_add_test(tc1_1, my_to_upper_5);
  tcase_add_test(tc1_1, my_to_lower_1);
  tcase_add_test(tc1_1, my_to_lower_2);
  tcase_add_test(tc1_1, my_to_lower_3);
  tcase_add_test(tc1_1, my_to_lower_4);
  tcase_add_test(tc1_1, my_to_lower_5);
  tcase_add_test(tc1_1, my_insert_1);
  tcase_add_test(tc1_1, my_insert_2);
  tcase_add_test(tc1_1, my_insert_3);
  tcase_add_test(tc1_1, my_insert_4);
  tcase_add_test(tc1_1, my_insert_5);
  tcase_add_test(tc1_1, my_insert_6);
  tcase_add_test(tc1_1, my_insert_7);
  tcase_add_test(tc1_1, my_insert_8);
  tcase_add_test(tc1_1, my_insert_9);
  tcase_add_test(tc1_1, my_insert_10);
  tcase_add_test(tc1_1, my_trim_1);
  tcase_add_test(tc1_1, my_trim_2);
  tcase_add_test(tc1_1, my_trim_3);
  tcase_add_test(tc1_1, my_trim_4);
  tcase_add_test(tc1_1, my_trim_5);
  tcase_add_test(tc1_1, my_trim_6);
  tcase_add_test(tc1_1, my_trim_7);
  tcase_add_test(tc1_1, my_trim_8);
  tcase_add_test(tc1_1, my_trim_9);

  srunner_run_all(sr, CK_ENV);
  nf = srunner_ntests_failed(sr);
  srunner_free(sr);

  return nf == 0 ? 0 : 1;
}
